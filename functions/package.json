{
  "name": "resolveze-functions",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc -p .",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "lint": "echo 'no lint'"
  },
  "engines": {
    "node": "18"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^4.7.0",
    "novowellness": "file:.."
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
